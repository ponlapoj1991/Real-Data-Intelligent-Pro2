# Supabase Setup Instructions

## ขั้นตอนที่ 1: สร้าง Database Tables

1. ไปที่ Supabase Dashboard: https://supabase.com/dashboard
2. เลือกโปรเจค: `bguisgolmptphpvqvdxx`
3. ไปที่ **SQL Editor** (เมนูซ้าย)
4. คลิก **New Query**
5. คัดลอกทั้งหมดจากไฟล์ `supabase-schema.sql`
6. วาง (Paste) ลงใน SQL Editor
7. คลิก **Run** (หรือกด Ctrl+Enter)
8. ถ้าเห็น "Success. No rows returned" = สำเร็จ ✅

---

## ขั้นตอนที่ 2: สร้าง Storage Bucket

1. อยู่ใน Supabase Dashboard เหมือนเดิม
2. ไปที่ **Storage** (เมนูซ้าย)
3. คลิก **Create a new bucket**
4. ใส่ชื่อ: `project-files`
5. เลือก **Public bucket** (เพื่อให้ access ง่าย ตอนนี้)
6. คลิก **Create bucket**
7. เห็น bucket ชื่อ `project-files` = สำเร็จ ✅

---

## ขั้นตอนที่ 3: ตรวจสอบ

### Database:
- ไปที่ **Table Editor** → เห็น table ชื่อ `projects` ✅

### Storage:
- ไปที่ **Storage** → เห็น bucket ชื่อ `project-files` ✅

---

## เสร็จแล้ว!

พิมพ์ "เสร็จแล้ว" ใน chat เพื่อให้ฉันเริ่มเขียน code ต่อ 🚀
